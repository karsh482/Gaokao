# Local Model Cache

This directory is reserved for local embedding model files.

The default API configuration uses:

```env
GAOKAO_EMBEDDING_CACHE_FOLDER=models
```

Large downloaded model files are ignored by Git.
